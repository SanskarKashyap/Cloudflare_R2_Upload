{
  "watch": ["src"],
  "ignore": ["src/**/*.test.ts"],
  "ext": "ts,mjs,js,json,graphql",
  "exec": "tsc && node --no-warnings --enable-source-maps -r dotenv/config ./build/server.js",
  "legacyWatch": true
}
